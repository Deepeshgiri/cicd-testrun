version: '3.9'

services:
  backend:
    build: .
    ports:
    - "0.0.0.0:3000:3000"

    environment:
      NODE_ENV: production
    restart: unless-stopped
    # Optional for dev, remove if you want container code to be immutable
    # volumes:
    #   - .:/app
